<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.renlingfei.heartware">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".BeneficiaryCampaignPage" />
        <activity android:name=".CollectorCampaignPage"/>

        <activity
            android:name=".BeneficiaryProfilePage"
            android:label="@string/title_activity_beneficiary_profile_page"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".StartCampaign"
            android:label="@string/title_activity_start_campaign"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true"/>
    </application>

</manifest>